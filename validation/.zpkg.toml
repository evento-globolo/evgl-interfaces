[package]
org = "evento-globolo"
name = "evgl-public-validation-contracts"
version = "0.1.0"
description = "Public peer validation authorities for evento-globolo"
license = "MIT"

[targets.json-schema]
dir = "."
adapter = "none"

[targets.typespec]
dir = "typespec"
adapter = "none"
